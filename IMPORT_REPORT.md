# مذكرة استيراد مشروع ZAVERRE

تم استيراد الفرع `main` من المستودع `alarbiehamoda-bit/zaverrecars` عند الالتزام `72dcf3e123358044c3218f885778150a2fde8e13`. تم التحقق من مطابقة ملفات التطبيق مع المصدر، مع استثناء ملفات بيئة العمل وسجل الاستيراد وملفات الاعتماديات والبناء المحلية.

يتكوّن المشروع من واجهة React/Vite، وخادم Express مع tRPC، وطبقة Drizzle لقاعدة البيانات. يضم المشروع مجلدات `client` و`server` و`drizzle` و`shared` و`scripts`، ولا يحتوي هذا الاستيراد على بيانات تجريبية أو منشأة يدويًا.

تم تثبيت الاعتماديات من ملف القفل الأصلي، وتشغيل الاختبارات الأصلية دون اتصال بقاعدة البيانات؛ نجح **78 اختبارًا ضمن 17 ملف اختبار**. كما نجح بناء الإنتاج. أُنشئت فقط جداول مخطط قاعدة البيانات من ترحيلات المصدر الست، من دون إدخال صفوف أو بيانات.

| متغير البيئة | الاستخدام |
|---|---|
| `DATABASE_URL` | اتصال قاعدة البيانات لمخطط Drizzle وبيانات الإدارة والحجوزات. |
| `JWT_SECRET` | توقيع الجلسات وملفات الارتباط. |
| `VITE_APP_ID` و`OAUTH_SERVER_URL` و`VITE_OAUTH_PORTAL_URL` | تهيئة مصادقة Manus OAuth. |
| `OWNER_OPEN_ID` | تعريف مالك المشروع وإسناد دور المدير. |
| `BUILT_IN_FORGE_API_URL` و`BUILT_IN_FORGE_API_KEY` | الوصول من الخادم إلى الخدمات المضمنة، بما فيها التخزين. |
| `VITE_FRONTEND_FORGE_API_URL` و`VITE_FRONTEND_FORGE_API_KEY` | الوصول المصرح به من الواجهة إلى الخدمات المضمنة. |
| `PORT` و`NODE_ENV` | تشغيل الخادم وفق بيئة التطوير أو الإنتاج. |

جميع المتغيرات المذكورة أعلاه متاحة من بيئة المشروع الحالية؛ لم تُطلب أي مفاتيح إضافية. المعاينة الأساسية تعمل، وأحدث طلبات واجهة tRPC العامة عادت بنجاح بحالة `200` بعد إنشاء الجداول الفارغة.

تمت مزامنة بيئة المشروع مع النسخة البعيدة قبل حفظ نقطة الاستعادة النهائية، ثم أُعيد التحقق من بقاء ملفات التطبيق مطابقة للمصدر.
